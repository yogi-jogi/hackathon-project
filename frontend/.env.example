# Frontend Environment Variables

# The base URL of the backend API. 
# Leave empty for local development if you are using Vite's proxy in vite.config.js
# For production (e.g., Vercel), set this to the deployed backend URL (e.g., https://your-backend.onrender.com)
VITE_API_URL=http://localhost:5000
